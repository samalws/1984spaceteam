// you have 5 pipes on your screen: 1 for each ministry, and 1 for memory hole
// also a pipe where things come in but that doesn't count because it's not an output pipe lol

// enum of all the pipes
const pipes = Object.assign({}, ministries, {memoryhole: 4})
